import { NaConvertPipe } from './na-convert.pipe';

describe('NaConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new NaConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
